# TAC-SM dependencies
# Install: pip install -r requirements.txt

# Core ML
torch>=2.2.0
# For Flash Attention (optional, install separately if needed):
#   pip install flash-attn --no-build-isolation
# PyTorch SDPA (used by default) is included in torch>=2.0

# Numerics
numpy>=1.26.0

# Optional: FAISS for large-scale memory retrieval
# faiss-cpu>=1.7.4   (CPU)
# faiss-gpu>=1.7.4   (GPU)

# Optional: HuggingFace tokenizers
# transformers>=4.40.0
# tokenizers>=0.19.0

# Training utilities
tqdm>=4.66.0

# Configuration
pyyaml>=6.0.1

# Experiment tracking (optional)
# wandb>=0.17.0
# tensorboard>=2.17.0

# Data processing (optional, for real corpus)
# datasets>=2.19.0

# Code analysis (for repository repair agent)
# tree-sitter>=0.22.0
